<Window x:Class="ReactiveGit.Demo.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ReactiveGit Scratch Pad"
        Height="300"
        Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="1" x:Name="clonePanel">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBox x:Name="cloneUrl" Margin="10" />

            <Button Grid.Row="1" Content="Clone Repository" Margin="10" x:Name="cloneRepository" />
        </Grid>
        

        <ContentControl x:Name="content" Grid.Row="2" />
    </Grid>
</Window>
